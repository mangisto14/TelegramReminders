services:
  bot:
    build: .
    restart: unless-stopped
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - DATABASE_PATH=/data/reminders.db
    volumes:
      - bot_data:/data

volumes:
  bot_data:
